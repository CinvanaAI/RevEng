"""RevEng's repository-analysis engine.

This package contains the static-analysis passes, semantic record model,
report composition, reusable capability packs, and built-in analysis workflows
used by the runtime.  Platform services expose and govern these capabilities;
the engine remains usable independently of the web UI.
"""
